import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ackerfin.flooded',
  appName: 'FLOODED - Survival & Rescue Offline System',
  webDir: 'dist'
};

export default config;
